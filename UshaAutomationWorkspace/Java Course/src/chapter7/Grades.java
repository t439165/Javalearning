package chapter7;

import java.util.Scanner;

public class Grades {

    /*private static int grades[];
    private  static Scanner scanner = new Scanner(System.in);

    public static void  main(String Args[]) {

        System.out.println("Enter how many grades you will enter:");
        grades = new int[scanner.nextInt()];
        getGrades(grades);

    }

    public  static int Grades() {

        for (int i = 0; i < grades.length; i++) {
            System.out.println("Enter  grades " + i+1);
            grades[i] = new int[scanner.nextInt(i)];
            return grades;

        }

        public  static void  getGrades(int[]grades){

            for (int i = 0; i < grades.length; i++) {
                System.out.println("Enter  grades " + i+1);
                grades[i] = new int[scanner.nextInt(i)];

            }

             public  static  int calculateSum(grades) {

                int sum=0;
                for (int value:grades) {
                    sum = sum + value;
                }
                return sum;

                }

            public  static  double calculateAverage(grades) {


            return calculateSum/grades.length;

                }

            public static int getHighest(){
                int highest = grades[0];
                for(int grade: grades){
                    if(grade > highest){
                        highest = grade;
                    }
                }
                return highest;
            }

            public static int getLowest(){
                int lowest = grades[0];
                for(int grade: grades){
                    if(grade < lowest){
                        lowest = grade;
                    }
                }
                return lowest;
            }
            }


            return calculateSum/grades.length;

                }
    }

     */

}